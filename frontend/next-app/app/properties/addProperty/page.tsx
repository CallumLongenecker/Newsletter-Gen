import React from "react";
import CreateForm from "./CreateForm";

const AddProperty = () => {
  return (
    <main>
      <h2 className="text-primary text-center">Add a New Property</h2>
      <CreateForm />
    </main>
  );
};

export default AddProperty;
